package functional_interfaces;

/*      | Interface           | Method                  | Description                                         |
        | ------------------- | ----------------------- | --------------------------------------------------- |
        | `Predicate<T>`      | `boolean test(T t)`     | Tests a condition and returns true/false            |
        | `Function<T,R>`     | `R apply(T t)`          | Takes one argument and returns a result             |
        | `Consumer<T>`       | `void accept(T t)`      | Takes one argument and returns nothing(do not return stream returns final value)              |
        | `Supplier<T>`       | `T get()`               | Supplies a result (no input)                        |
        | `BiFunction<T,U,R>` | `R apply(T t, U u)`     | Takes two arguments and returns a result            |
        | `BiConsumer<T,U>`   | `void accept(T t, U u)` | Takes two arguments, no return                      |
        | `UnaryOperator<T>`  | `T apply(T t)`          | Same input and output type                          |
        | `BinaryOperator<T>` | `T apply(T t1, T t2)`   | Takes two same-type arguments and returns same type |

 */


public class intro {

}
